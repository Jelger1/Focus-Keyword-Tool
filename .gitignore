node_modules/
.vercel/
.env
.env.local
.env*.local
.DS_Store

# Lokale back-ups van VS Code (Local History), bevatten ook kopieën van .env.local
.history/

# Video en plan uit de /brag-sessie: geen onderdeel van de tool
brag-output/

# Google Service Account-sleutels: horen in .env.local en Vercel, nooit in git of een deploy
api/*.json
*service-account*.json
pureminds-warehouse-*.json
